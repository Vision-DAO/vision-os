name: Hardhat Contract and Beacon DAO Module Tests
on: [push]
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
      - name: Use Node.js 16
        uses: actions/setup-node@v3
        with:
          node-version: 16
          cache: 'yarn'
      - run: yarn install
      - run: cargo install cargo-make
      - run: cargo install wasm-pack
      - run: cd fixtures/modules && cargo make && cargo make test
      - run: yarn hardhat test
